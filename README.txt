'''SEARCH ENGINE INSTRUCTIONS'''

1. Place folder containing files to be indexed in same directory as main.py

2. In main.py, change the subdomain text from "/DEV" to the name of the folder containing files to be indexed, if necessary.

3. Run main.py and choose to 'BUILD' a new index of the requested files or 'SEARCH' a pre-existing one.
    3a. After an index has been built or if searching an index, input desired search terms.

